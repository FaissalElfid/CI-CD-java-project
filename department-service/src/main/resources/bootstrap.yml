spring:
  cloud:
    config:
      uri:  "${ CLOUD-CONFIG-SERVER-SVC: http://localhost:9296/}"
      label: main
      profile: default
      fail-fast: true
      retry:
        max-attempts: 30
        max-interval: 8000